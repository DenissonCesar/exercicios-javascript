const valores = [1, 2, 3, 4]

for (let i = valores.length - 1; i >= 0; i--) {
    console.log(valores[i])
}

//push, pop, unshift, shift, splice, slice, concat

// for of, for in, forEach

// Objeto: é uma par de chave e valores